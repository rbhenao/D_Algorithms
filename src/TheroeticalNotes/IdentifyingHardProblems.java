package TheroeticalNotes;

public class IdentifyingHardProblems {
	/*telecommunications problem
	 * 
	 * min_devices = number of communication centers (worst case)
	 * for each assignment of (0,1) to the communication centers
	 * 0 = no device 1 = device
	 * if assignment is valid aka all cables are monitored
	 * number of devices = number of 1s in asignment
	 * min_devices = min(min_devices, number of devices)
	 */
}
